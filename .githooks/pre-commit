#!/bin/sh
# Fail fast
set -e

echo "[pre-commit] Running fast checks (fmt, clippy, unit tests)..."

# Rust fmt/clippy/tests only; defers the wasm build + web test to pre-push to stay fast
npm run check:rust >/dev/null

echo "[pre-commit] OK"
